# 代码与发布隐私 Review

## 本次已修复

- 文档与设计索引包含真实站点和设计文件关联信息。已将这些信息及私有部署脚本移出 Git 跟踪，留在本地忽略路径；仓库只保留通用使用说明、UI偏好与素材。
- 验收脚本曾有真实生产目标的默认值，并检查无关站点。现要求显式提供目标地址，只验收目标游戏；SSE首次读取增加15秒超时，避免连接异常时一直等待。
- 原始提交历史包含已移出的信息。重新建立没有父提交的发布历史，推送前检查所有可达 Git 对象；不能用普通删除文件提交代替历史清理。
- 新增本地敏感词清单与提交/推送检查，覆盖暂存区、可达提交历史、文本、二进制原文及PNG压缩文本元数据。检查失败只输出类别和对象编号，不输出私密原文。

## 已检查的代码路径

- 服务端令牌认证、房间权限、版本冲突、骰子权威、请求幂等与SSE同步。
- 静态资源允许列表、请求来源检查、请求频率和房间上限、存档中令牌哈希。
- 共享规则引擎的双色归属、跳跃、飞行、撞机、叠子、终点反弹与排名。
- 前端移动阶段与朝向、连续状态排队、减少动态效果处理。此次未修改游戏规则或UI行为。

## 验证与边界

- 39项测试通过，JavaScript语法检查和构建通过；新增验证包括私密词检测、PNG压缩元数据检测、验收脚本缺少目标时拒绝执行。
- 新历史需通过 `node tools/privacy-check.mjs --history`；远端推送后再用全新克隆验证。
- 隐私检查依赖仅保存在本地的敏感词清单；它不能识别所有语义关联或代替人工审核图片内容。
- 本次为源码与发布隐私检查，未执行渗透测试、并发压测或真实移动设备测试，也未重新部署生产服务。
