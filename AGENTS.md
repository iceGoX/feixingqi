# 飞行棋项目协作规范

## 项目范围

- 本目录是独立的 `feixingqi` 仓库，支持 `/feixingqi/` 子路径部署。
- 所有读取、编辑、测试和 Git 操作都在本目录执行，不在父目录操作 Git，不修改其他网站。
- 开始前阅读本文件、`README.md`、`rule.md`、`RULES_IMPLEMENTED.md`，检查 `git status --short --branch`，保留用户已有修改。
- 默认直接推进明确授权的工作；开发中不反复询问常规实现选择，完成后统一汇报。涉及缺失凭据或真实阻塞时如实说明。
- 文本和代码文件末尾不要增加空白行。

## 已确认的 UI 偏好

- 产品名称始终是「飞行棋」。整体采用温暖奶油底色、薄荷绿主按钮、柔和圆角、清晰中文文字和适量留白。
- 大厅和等待页偏 Q 版玩具风：圆润可爱的小飞机、骰子与轻盈航迹。避免冷硬军事风、细长客机或像鱼雷的飞机。
- 棋盘飞机采用四色同款 Q 版俯视造型：圆润机身、宽机翼、白色风挡；使用 `assets/planes-q-atlas.jpg`，不是旧的细长飞机素材。
- 图集为等分 2×2，依次为左上红、右上黄、左下蓝、右下绿；原始机头朝上。素材来源与提示词见 `design/aircraft-q.md`。
- 实际在场飞机必须清晰、不透明，保留原本颜色。不得通过 `mix-blend-mode:multiply` 与彩色跑道混合，也不能因禁用点击而降低飞机透明度。可用小面积浅色衬底和柔和阴影增强分离度。
- 半透明仅用于落点预览；完成后的棋子可淡化，但不能把正常飞机渲染成预览效果。
- 机头必须与当前移动方向一致：每段先转向目标再移动，拐弯、同色跳跃、飞行捷径、终点折返、撞回机场都要正确朝向。编号始终正向，不跟随旋转。
- 普通移动逐格播放并稍作停顿，不能直接瞬移；跳跃与捷径按实际路径播放。完成动画后再开放行动或展示结算。
- 棋盘轨迹使用「彩色格底 + 中间圆形留白」，不要换成实心彩色圆点，也不要只画难以辨认的细空心圆。
- 棋盘结构以规则和坐标数据为准，不根据生成图片重画拓扑。保持四角机场、52格环道、四色终点航道与四条正交飞行捷径，不能改成对角 X 航线。
- 桌面、平板和手机都要能操作，棋盘保持正方形，无横向溢出；缩小后仍能分辨飞机与编号。
- 大幅视觉变化先给预览；已确认的方向直接实现。用户要求同步设计稿时，先改 Pixso，再使用同一套素材实现网页。

## Pixso 设计基准

- 文件名「飞行棋」，包含大厅、候场、三端对局、规则、结算与素材规范等画板。
- 真实设计文件链接、文件夹、节点索引仅保存在被 Git 忽略的本地配置中，不能上传。
- 操作前通过本地索引验证目标文件；不要修改其他游戏的设计稿。
- `design/pixso-export/` 是本地生成参考，不作为运行代码来源直接覆盖交互。

## 实现约束

- 使用原生 HTML/CSS/JavaScript 和 Node.js 内置模块；无明确收益不引入框架、数据库或运行依赖。
- `shared/board.js` 是棋盘坐标、颜色、航道和飞行线的唯一来源。
- `shared/engine.js` 是在线与同屏共用的规则引擎；`shared/motion.js` 只负责移动阶段展开、朝向和展示队列。
- 以用户当前 `rule.md` 为准，补充执行约定写入 `RULES_IMPLEMENTED.md`。不要擅自恢复用户已删除的规则，例如三连六惩罚。
- 双人模式每人控制两种相对颜色、共8架飞机；同一玩家不互撞，不同颜色不能叠成一组。
- 在线骰子、行动合法性、回合、碰撞和胜负由服务端决定，不能在前端绕过校验。
- 保持玩家令牌认证、`expectedVersion`、`requestId` 幂等及 SSE 同步；重复响应不应重播或打断动画，连续版本排队，断线跨版本后同步真实状态。
- 正在播放动画时锁定行动；后台标签页及减少动态效果设置可直接同步当前状态。
- 新运行文件同时检查服务端静态允许列表、`tools/build.js` 和 `sw.js` 缓存清单；所有资源需支持 `/feixingqi/` 子路径。

## 验证

```bash
npm run check
npm test
npm run build
npm run dev -- --host 127.0.0.1 --port 4187
```

- UI 修改至少检查桌面、834×1194平板和390×844手机布局；鼠标和触控点击目标都要可用。
- 飞机可见度要在机场及红、黄、蓝、绿跑道上检查，包括当前玩家、其他玩家、选中、移动、叠子和落点预览状态。
- 朝向或动画修改覆盖普通走步、转角、起飞、同色跳跃、飞行、终点折返；在线双方结果必须一致，不能只验证同屏模式。
- 保持测试有实际行为价值；单纯低风险样式修改以浏览器视觉检查为主，不新增镜像 CSS 的测试。
- 区分本地测试、浏览器验证、真实设备验证、提交、推送、服务器部署、公网验收，不把前一项当成后一项。

## GitHub 与发布

- GitHub 账号为 `iceGoX`，仓库名为 `feixingqi`。个人提交使用 `105399284+iceGoX@users.noreply.github.com`，使用前检查有效身份。
- 仅在用户授权时提交、推送和部署；检查暂存区实际文件与 diff，不能上传凭据、真实环境文件、私钥、房间存档、缓存或发布临时包。
- `dist/`、`.data/`、`artifacts/`、环境文件、日志、临时 Pixso 导出与废弃设计草图仅留本地；保留运行源代码、当前素材、规则、通用设计规范与测试。
- 真实域名、IP、主机名、部署实例、Nginx站点配置、设计文件链接均为私密信息，不能出现在源码、文档、截图、提交历史、仓库描述或主页字段中。私有仓库也不例外。
- 私有部署脚本与实际配置仅保留在本地忽略路径，执行前通过本地文档核对服务版本、实例和路由。
- 使用构建产物的 `release.json` 和 SHA-256 验证同一版本，保留回滚能力，不覆盖服务器房间存档，不改其他网站路由。
- 部署后运行 `node tools/public-smoke.mjs "$FEIXINGQI_URL"`，检查服务、Nginx、公网资源、API/SSE，并确认已有网站可用。

- 每次提交运行 `npm run privacy:check`，推送前运行 `node tools/privacy-check.mjs --history`；具体禁止上传的域名/标识仅存在 `.local/privacy-terms.json`，不得把敏感原文写入检查脚本或测试。
- 本地启用 `.githooks` 的提交/推送检查。历史含私密信息时，先清理可达历史，不能只删除当前文件后继续推送旧提交。
